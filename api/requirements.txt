fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlmodel==0.0.39
pydantic-settings==2.7.0
httpx==0.28.1
python-multipart==0.0.20
numpy==2.2.1
scikit-learn==1.6.0
# Postgres driver. SQLite needs nothing, so this only matters in production —
# but it stays pinned here rather than in a separate file, because pip installs
# a requirements file all-or-nothing and a version without a wheel for your
# Python takes every other package down with it.
psycopg[binary]==3.3.4

pytest==8.3.4
pytest-asyncio==0.25.0

# Voice is optional and heavy — see requirements-voice.txt
